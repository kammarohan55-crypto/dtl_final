services:
  - type: web
    name: dtl-web-app
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn prod_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
